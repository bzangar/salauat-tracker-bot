spring:
#  sql:
#    init:
#      mode: always
#      platform: postgres
  datasource:
    url: jdbc:postgresql://localhost:5432/salauat_bot
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

telegram:
  token: ${BOT_TOKEN}
  name: ${BOT_USERNAME}

logging:
  level:
    org.springframework.cache: TRACE